require "spec_helper"

RSpec.describe DatabaseI18n do
  it "has a version number" do
    expect(DatabaseI18n::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
